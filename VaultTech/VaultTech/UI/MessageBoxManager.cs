/*
 * <Copyright>
 * Owned by:- Vault 16 Software
 * Author:- Mustafa Al-Sibai
 * Date Created:- 12/June/2014
 * Date Moddified :- 18/January/2015
 * </Copyright>
 */

using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace VaultTech.UI
{
    public class MessageBoxManager
    {
        /* static GraphicsDeviceManager Graphics;
         static string StyleFilePath, ComponentNodeNameInXml;

         static List<MessageBox> messageBox;
        
         public MessageBoxManager()
         {
             messageBox = new List<MessageBox>();
         }

         public static void Show()
         {
             messageBox.Add(new MessageBox());

             messageBox[messageBox.Count - 1].Initialize(Graphics);
             messageBox[messageBox.Count - 1].LoadContent(StyleFilePath, ComponentNodeNameInXml);
             messageBox[messageBox.Count - 1].UpdateOnce();
         }

         public void Initialize(GraphicsDeviceManager Graphics)
         {
             MessageBoxManager.Graphics = Graphics;
         }

         public void LoadContent(string StyleFilePath, string ComponentNodeNameInXml)
         {
             MessageBoxManager.StyleFilePath = StyleFilePath;
             MessageBoxManager.ComponentNodeNameInXml = ComponentNodeNameInXml;
         }

         public void UpdateOnce()
         {
         }

         public void Update(GameTime gameTime)
         {
             for (int i = 0; i < messageBox.Count; i++)
                 messageBox[i].Update(gameTime);
         }

         public void Draw(SpriteBatch spriteBatch)
         {
             spriteBatch.Begin(SpriteSortMode.Immediate, BlendState.AlphaBlend);

             for (int i = 0; i < messageBox.Count; i++)
                 messageBox[i].Draw(spriteBatch);

             spriteBatch.End();
         }*/
    }
}